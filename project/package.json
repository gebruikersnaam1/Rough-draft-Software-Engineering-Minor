{
  "name": "se_typesafe",
  "version": "1.0.0",
  "description": "Building my own version of LINQ",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build"
  },
  "author": {
    "name": "Jurgen van der Heiden"
  },
  "license": "ISC",
  "dependencies": {
    "tslint": "^6.1.2",
    "typescript": "^3.8.3"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts",
  "devDependencies": {
    "@types/node": "^13.13.5"
  }
}
